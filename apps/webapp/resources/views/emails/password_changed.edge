@component('emails/layout', { title: 'Your password was changed' })
  <p style="margin: 0 0 16px;">Hi {{ name }},</p>

  <p style="margin: 0 0 16px;">
    The password of your {{ brandName }} account was just changed. Every other
    session and every browser extension token was signed out.
  </p>

  <p style="margin: 0;">
    If you did not make this change, reset your password immediately from
    <a href="{{ appUrl }}" style="color: #18181b;">{{ appUrl }}</a>.
  </p>
@end
