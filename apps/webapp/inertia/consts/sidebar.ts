/**
 * A rail row has to read as one more button in the column that starts with the
 * toggle and the search icon, so it takes the same 38px square a default
 * `IconButton` occupies (`p-2` around a 20px icon, plus its border).
 */
export const RAIL_ITEM_CLASS = 'justify-center px-0 h-[38px] w-[38px] mx-auto';
